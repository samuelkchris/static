// custom.js

$(document).ready(function () {
    // Example: Initialize select2 for better dropdowns
    $('.select2').select2();

    // Example: Initialize flatpickr for date pickers
    $('.flatpickr').flatpickr();
});
